#include <main.h>
#include <string>
#include <iostream>

struct TagStruct{
    std::string name;
    int pairs;
    std::string text;
};

int RBLCAM001::main(std::string argc, char * argv[]){
    while(true){
        
    }
}
